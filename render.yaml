services:
  - type: web
    name: luxestay-hotel-management-system
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn luxestay.wsgi:application"